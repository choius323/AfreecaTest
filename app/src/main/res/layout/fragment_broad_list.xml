<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <com.google.android.material.circularreveal.CircularRevealFrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.broad.BroadListFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_broad"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_rv_broad" />

        <TextView
            android:id="@+id/tv_empty_broad_list"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/empty_broad_list"
            android:textSize="23sp"
            android:textStyle="bold"
            app:drawableTopCompat="@drawable/ic_exclamation" />

    </com.google.android.material.circularreveal.CircularRevealFrameLayout>
</layout>